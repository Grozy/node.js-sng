<!--<!DOCTYPE html>-->
<%-include partials/header%>
<!--<% posts.forEach(function(post, index){ %>
  <p>
    <h2><a href="/topic/<%= post.id %>"><%= post.title %></a></h2>
  </p>
  <p class="info">
    作者：<a href="#"><%=post.name%></a> |
    日期：<%= post.time_stamp %>
  </p>
  <p>
    <%- post.post %>
  </p>
<% }) %>-->
<%-include partials/footer%>
